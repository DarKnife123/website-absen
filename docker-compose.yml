version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5010:5010"
    environment:
      - DATABASE_URL=mongodb://mongo:5110/Presensi?replicaSet=rs0
      - AUTH_SECRET=${AUTH_SECRET:-nrehivnajrnauirgnuoaeaovnraviyerbvain}
      - NEXTAUTH_URL=${NEXTAUTH_URL:-http://localhost:5010}
    volumes:
      - uploads_data:/app/public/uploads
    depends_on:
      mongo_setup:
        condition: service_completed_successfully

  mongo:
    image: mongo:6
    command: ["--replSet", "rs0", "--bind_ip_all", "--port", "5110"]
    ports:
      - "5110:5110"
    volumes:
      - mongo_data:/data/db

  mongo_setup:
    image: mongo:6
    depends_on:
      - mongo
    volumes:
      - ./scripts:/scripts
    command: >
      bash -c "
      echo 'Waiting for MongoDB to start...';
      sleep 5;
      mongosh --host mongo:5110 --eval '
      try {
        rs.status();
        print(\"Replica set already initialized\");
      } catch (err) {
        if (err.codeName === \"NotYetInitialized\") {
          rs.initiate({
            _id: \"rs0\",
            members: [{ _id: 0, host: \"mongo:5110\" }]
          });
          print(\"Replica set initialized\");
        } else {
          print(\"Error checking status: \" + err.message);
        }
      }'"

volumes:
  mongo_data:
  uploads_data:
